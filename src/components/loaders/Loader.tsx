const Loader = () => {
  return (
    <div className="w-screen h-screen bg-[var(--bg)] flex items-center justify-center">
      <span className="loader"></span>
    </div>
  );
};

export default Loader;
